# UI Menu Sample

Build a menu scene with:

- at least three focusable buttons
- keyboard and mouse navigation
- obvious focus styling
- a stable initial focus on `StartButton`

Success looks like:

- `inspect.focus` reports `StartButton` first
- the agent can move focus to another control
- a screenshot clearly shows the highlighted target
- hover and focus inspection match what is visible

